## Docker Note

Due to time constraints, Docker was not executed locally. However, a valid Dockerfile is included and the project is fully runnable using the provided command.

The Docker setup follows standard Python containerization practices and should work in any Docker-enabled environment.